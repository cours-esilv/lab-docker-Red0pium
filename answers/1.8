"docker run -p 5000:5000 -e CURRENT_ENVIRONMENT=development mathysbnc/lab-docker"

before the image starts, it says "Unable to find image 'mathysbnc/lab-docker:latest' locally"
then proceeds to pull the image from DockerHub
and then runs it

"docker run -dp 5000:5000 -e CURRENT_ENVIRONMENT=development mathysbnc/lab-docker"